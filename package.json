{
  "name": "@streatcodes/silk",
  "version": "0.0.6",
  "description": "A Preact component library",
  "files": [
    "components",
    "index.tsx"
  ],
  "scripts": {
    "dev": "esbuild kitchen-sink.tsx --bundle --outfile=dist/bundle.js --external:*.woff2 --serve=localhost:8080 --servedir=.",
    "prod": "esbuild kitchen-sink.tsx --bundle --outfile=dist/bundle.js --external:*.woff2 --minify"
  },
  "author": "Matthew Streatfield",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.25",
    "preact": "^10.6.6"
  }
}